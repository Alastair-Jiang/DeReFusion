# 两个 OpenClaw 实例之间的任务分配（提案）

**日期：** 2026-09-14 · **状态：** 提案（未执行任何结构调整）
**适用对象：** 本机实例 **希伯来 / ThinkBook Ultra9 285H**（16 逻辑核 / 31.5GB / **无可用外网出口** / 持有 DeReFusion 克隆 + venv + 数据集 + 自动化）
与第二个实例（见 §0 待确认）。

---

## 0. 先确认"第二个实例"是谁（这决定分配细节）

| 候选 | 判别依据 | 若为它，分工重点 |
|---|---|---|
| **geng / 联想 83JM** | 现有常驻协议已规定"计算任务默认整套给 geng"；无独显、无 WSL、Windows 11 | CPU 型可整包外派的计算；**不能跑** GPU / 需 Linux 的任务 |
| **董学九 / Mac Mini M4** | M4 有较强 CPU/GPU（Metal），macOS | 可作为第二个算力点 + **外网出口**候选 |
| **桂鱼 / WSL2** | Linux 环境 | 需要 Linux 的任务、跨平台验证 |
| **daft / NAS** | 存储为主 | 数据落盘 / 大文件托管，不宜做算力 |
| **本机两个 agent（`main` 与 `openclaw`）** | 同一安装内的两个身份，**共享同一 CPU/内存/仓库** | 只能按"角色"分工（执行者 / 校验者），**不能按算力分工** |

> **需要你一句话确认**。下面的规则对任意组合都成立，只是 §3 的具体派发会随组合调整。

---

## 1. 能力不对称（这是分工的全部依据）

| 能力 | 希伯来（本机） | 第二实例（典型） |
|---|---|---|
| DeReFusion 克隆 + venv + 41 个数据集 | ✅ 唯一持有 | ❌（除非克隆） |
| 实验自动化 / 计划任务 / 自愈启动器 | ✅ 已在跑 | ⚠️ 需另建，**极易重复触发** |
| **外网出口**（Yahoo / Stooq / 文献） | ❌ 代理坏 + 直连 403 / JS 挑战 | **可能 ✅** ← 这是最可能的分工点 |
| 长期无人值守跑批 | ✅（但进程被静默杀死过 3 次） | 依机器而定 |
| 证据链与裁决的作者身份 | ✅ 规则的作者（**结构上不适合自审**） | ✅ 天然独立 → 适合做校验 |

---

## 2. 五条不可协商的规则（都来自已经踩过的坑）

1. **单一写者（single writer）**：一份产物只有一个写者。仓库、账本、状态文件各自指定唯一 owner；另一方只读、只提"提议"，不直接改。
   - *踩过的坑*：Codex 的克隆 `origin/main` 引用过期 → 两份额外分叉；本地仓库由我独写才不会打架。
2. **能力归属，而非便利归属**：任务给"那台机器能力对得上"的一方，而不是"顺手"的一方（例如取数给出网通的一方）。
3. **长任务加锁 + 幂等**：同一批实验不得两个实例同时跑。
   - *踩过的坑*：批处理曾出现重复实例；后来用"有 run 在跑就退出"的自愈启动器解决——**这个锁必须由任务 owner 持有**。
4. **交付物 = 产物 + 提交哈希 + 账本条目**，不用聊天内容当凭证。
   - *踩过的坑*：口头/聊天式交接导致"未完成项被当成已完成"。
5. **校验是独立角色**：校验方只依据**原始产物**（raw CSV/JSON/npy）复算，不依据对方的总结；且校验范围要**事先写死**，避免"挑着验"。

---

## 3. 按当前待办的具体派发

| 待办 | 归属建议 | 交付物 | 验收 |
|---|---|---|---|
| **C1 独立前瞻队列取数**（Yahoo/Stooq，本机被墙） | **第二实例**（哪台出网通给哪台） | 单一来源的 15–20 个资产 CSV + 每文件 SHA-256 + 来源说明；提 PR/分支 | 我核对：行数、2016-01-01→2025-12-31 覆盖、去重、单一来源、哈希一致 |
| **F1 及后续容量面板的重算校验** | **第二实例** | 从 `operator-regime-capacity_f1.csv` 独立复算逐资产 mean ΔMSE 与"稳定/不稳定"判定 | 与 `21_f1_result.md` 逐格比对；不一致则出差异清单 |
| **文献核验积压**（`ns_related_work_map.md` 的引用未经在线核验） | **第二实例**（需网络） | 逐条给出：真实标题/作者/年份/venue 或"未找到" | 我的核对清单全项有结论，含反例 |
| **重算密集型新实验**（如未来的 C1 框架训练 90–120 次） | **按能力**：若第二实例是 M4 → 部分外派；若是 83JM → 只能承接 CPU 型小面板 | 原始产物 + 运行清单 | 协议一致（种子/切分/指标不可改） |
| **NS 草稿 runner 的泄漏审计**（仅在复活时） | **第二实例**（我写过相关评审 → 需独立性） | 逐行审计报告：是否有测试期信息进入算子/探测器 | 审计项清单先行锁定 |
| **仓库与证据链的主笔** | **希伯来（本机）**，唯一写者 | 提交 | 另一方只读 |

---

## 4. 最小落地机制（不引入新系统）

1. **产物归属账本**：沿用 lobster-link 的 `TASK-LEDGER.md` 形式，新增一节"DeReFusion 产物 → owner → 状态"，编号权仍在我。
2. **交接协议**：`HANDOFF-<n>.md`（要求方写：目标 / 交付物 / 验收判据 / 截止）+ `RECEIPT-<n>.md`（执行方写：状态 / 证据 / 未做部分）。**不用聊天内容做凭证**。
3. **锁**：长任务由 owner 持锁（本机已是"有 run 在跑就跳过"的自愈启动器；外派任务由对方持锁并在账本登记）。
4. **校验请求模板**：只能引用**原始产物路径 + 事先写死的校验项**。
5. **停止门**：任一方的产物若未按协议交付 → 暂停向它派新任务并出 `*-EXCEPTION.md`（沿用现有规程）。

---

## 5. 不建议的做法（会直接复现已发生的故障）

- ❌ 两个实例同时写同一仓库/同一克隆（分叉、丢改动、提交冲突）
- ❌ 两个实例都对同一批实验设自动触发（重复跑、CPU 互相拖死）
- ❌ 让校验方基于"总结"验收（等于自审）
- ❌ 把重 GPU / 需 Linux 的任务派给 83JM（无独显、无 WSL）
- ❌ 让规则作者给自己做独立校验（结构上无效）

---

## 6. 需要你确认的两件事

1. **"第二个 OpenClaw"具体是哪台/哪个身份？**（geng / 董学九 / 桂鱼 / daft / 本机另一个 agent）
2. 是否要我把 §4 的机制**实际落地**（建账本节 + 交接模板），还是先作为提案放着。
